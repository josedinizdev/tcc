import styled from "styled-components"

const StyledHeaderBar = styled.section `
    body {
        background-color: #242424;
        padding: .4rem 3rem .4rem 2rem;
        width: 100%;
    }   

    img {
        width: 4.7rem;
        height: auto;
    }

    img:nth-child(2) {
        width: 8rem;
        height: auto;
        margin-bottom: 8.2%;
    }

    a {
        color: #fff;
        font-family: inria-regular;
        font-size: 1.2rem;
        margin-right: .6rem;
    }


`;

export default StyledHeaderBar;